# ============================================
# Student Dropout Risk Prediction - Dependencies
# ============================================

# Core ML
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.1.0
xgboost>=1.6.0
imbalanced-learn>=0.10.0
joblib>=1.2.0

# Web Backend
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=22.0.0

# Authentication + database (Firebase Authentication + Cloud Firestore).
# NOTE: intentionally no firebase-storage dependency - Firebase Storage is not used.
firebase-admin>=6.5.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional: Terminal Dashboard
rich>=12.0.0
